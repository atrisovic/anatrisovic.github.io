domain: anatrisovic.com       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://anatrisovic.com  # the base hostname and protocol for your site, e.g. http://example.com
# baseurl: /jekyll/_site      # place folder name if the site is served in a subfolder